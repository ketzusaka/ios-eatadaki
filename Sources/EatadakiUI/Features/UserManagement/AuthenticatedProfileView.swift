import SwiftUI

struct AuthenticatedProfileView: View {
    var body: some View {
        List {
            Text("Profile")
                .font(.headline)
        }
        .navigationTitle("Profile")
    }
}
